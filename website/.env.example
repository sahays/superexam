# Service URLs
# Local: http://localhost:8000
# Prod: https://api-service-xyz.run.app
PROCESSING_SERVICE_URL=http://localhost:8000

# Google Cloud Storage (Optional for local dev)
# If provided, uploads go to GCS. If empty, local filesystem is used.
GCS_BUCKET_NAME=

# API Keys
GEMINI_API_KEY=

# Authentication
# Generate a secure random string: openssl rand -base64 32
JWT_SECRET=your-secret-key-change-in-production

ADMIN_CODE=ADMIN-CHANGE-THIS-CODE

# Google Cloud Project (Optional, auto-detected in Cloud Run)
GCP_PROJECT_ID=
